@page
@model RazorPagesMedicalCenter.Pages.Physicians.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Physician[0].PhysicianFName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Physician[0].PhysicianLName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Physician) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PhysicianFName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhysicianLName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PhysicianID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PhysicianID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PhysicianID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
