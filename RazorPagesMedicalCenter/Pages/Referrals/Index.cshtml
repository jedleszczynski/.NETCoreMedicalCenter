@page
@model RazorPagesMedicalCenter.Pages.Referrals.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Referral[0].ReferralID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Referral[0].Origin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Referral) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReferralID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Origin)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ReferralID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ReferralID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ReferralID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
